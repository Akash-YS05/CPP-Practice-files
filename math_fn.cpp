#include <iostream>
#include <cmath>

int main() {
    double x = 3.5;
    double y = 4;
    double z;

    z = std::max(x, y);
    z = std::min(3, 4);
    z = pow(3, 4);
    z = sqrt(16);
    z = ceil(x);
    z = floor(x);
    z = abs(-9);

    return 0;


}
